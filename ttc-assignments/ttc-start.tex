%
% LATEX PREAMBLE FOR TRINITY THEOLOGICAL COLLEGE ASSIGNMENTS
%
% Authors: Sam Rae
%

%
% PDF features
%
\usepackage[pdftex, pdfproducer={LaTeX}, pdfcreator={pdflatex}]{hyperref}

%
% Page layout
% Trinity specifications: left margin 3cm, right margin 2cm (as of 2013)
%
\usepackage[top=3cm, bottom=3cm, left=3cm, right=2cm]{geometry}


%
% Typesetting
%

% Fonts
\usepackage{xltxtra} % Includes fontspec
\defaultfontfeatures{Numbers=OldStyle,Scale=MatchLowercase}

\setmainfont[Mapping=tex-text}{Times New Roman}

% If you have the Times New Roman Std font from Monotype, use this line instead
%\setmainfont[Mapping=tex-text,ItalicFont={Times New Roman Italic},BoldFont={Times New Roman Bold}]{Times NR MT Std}

% Fonts - Greek
\newfontfamily{\greek}[Script=Greek]{Linux Libertine O}
\newcommand{\gr}[1]{{\greek #1}}

% Fonts - Hebrew
\usepackage{bidi} % Provides functionality for bi-directional typesetting (e.g. Hebrew within English)
\newfontfamily{\hebrew}[Script=Hebrew]{SBLHebrew}
\newcommand{\heb}[1]{\RL{\hebrew #1}}

% Spacing
\usepackage{setspace}
\setstretch{1.3} % Equivalent of 1 1/2 line spacing
\setlength{\parskip}{4pt}

% Lists
\usepackage{enumerate} % Numbered lists

% Headings
\setcounter{secnumdepth}{-1} % Remove numbers from section headings

% Sets the labels in definition lists to not bold (e.g. for translation comparisions in an exegesis paper)
\usepackage{enumitem}
\setdescription{font=\normalfont}

%
% Functions for college papers
%
% Easy small capsing of translations and names for God
\newcommand{\tr}[1]{\textsc{#1}} % General (less-used) translations
\newcommand{\tniv}{\tr{tniv}}
\newcommand{\niv}{\tr{niv}}
\newcommand{\nrsv}{\tr{nrsv}}
\newcommand{\lxx}{\textsc{lxx}}

\newcommand{\nt}{\textsc{nt}}
\newcommand{\ot}{\textsc{ot}}


\newcommand{\lord}{\textsc{Lord}}
\newcommand{\god}{\textsc{God}}
\newcommand{\yhwh}{\textsc{yhwh}}


% Bible formatting
\newcommand{\vs}[1]{\textbf{\small #1}} % Format verse numbers


%
% Environments for College papers
%

% Abstract
\usepackage{abstract}
\renewcommand{\abstracttextfont}{\normalfont}

% Essay Question
\newcommand{\question}[1]{{\center{\section{#1}\vspace{2cm}}}}

% Translation
\usepackage{color}
\usepackage{framed}
\definecolor{shadecolor}{rgb}{0.9,0.9,0.9}

\newenvironment{translation}
{
	\renewcommand{\abstractname}{Translation}
	\begin{shaded}
	\begin{abstract}
}
{
	\end{abstract}
	\end{shaded}
}

% Text comparison
\newenvironment{text-comparison}
{
	\begin{shaded}
	\begin{description}
}
{
	\end{description}
	\end{shaded}
}

% Poetry (especially for translations)
\usepackage{alltt} % Similar environment to verbatim
\newenvironment{poetry}
{
	\begin{verse}
	\begin{alltt}
	\normalfont{}
}
{
	\end{alltt}
	\end{verse}
}

% Scripture citation
\newenvironment{scripture-citation}
{
   \vfill
   \begin{center}
   \small
}
{
   \end{center}
}


%
% Referencing
%
\usepackage[elide]{natbib}
\bibliographystyle{/Users/samrae/Dropbox/College/References/Trinity}
\setcitestyle{aysep={},notesep={:}}
\renewcommand\refname{Bibliography}


%
% Document Info
%
\input{mmd-title}
\title{\mytitle}
\author{\myauthor}
\date{\mydate}


%
% Begin document
%
\begin{document}